include 'tests'

rootProject.name = 'template-messagehub-trigger'

gradle.ext.openwhisk = [
  version: '1.0.0-SNAPSHOT'
]

gradle.ext.scala = [
  version: '2.11.8',
  compileFlags: ['-feature', '-unchecked', '-deprecation', '-Xfatal-warnings', '-Ywarn-unused-import']
]
